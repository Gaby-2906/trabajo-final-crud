# CRUD de Productos en PHP y MySQL

Sistema de gestión de productos que utiliza PHP, MySQL y XAMPP.

## Instalación
1. Colocar en la carpeta htdocs.
2. Crear la base de datos `crud_db` y la tabla `productos`.
3. Acceder en el navegador: `http://localhost/trabajo-final/`